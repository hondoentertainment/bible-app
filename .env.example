# Server-side only — never exposed to the browser.
# Get a free key at https://scripture.api.bible/signup
#
# Local dev: copy to .env and set BIBLE_API_KEY
# Vercel: Project → Settings → Environment Variables → BIBLE_API_KEY
BIBLE_API_KEY=your_api_key_here
